---
# roles:
# - geerlingguy.glusterfs
# - bertvv.bind
# - bertvv.dhcp # not updated anymore
# - src: https://github.com/bertvv/ansible-role-dhcp.git
#   name: bertvv.dhcp

collections:
- ovirt.ovirt
# - gluster.gluster
- ansible.posix
- community.vmware
- community.kubernetes
- community.okd
- community.windows
- community.general

# Install with:
# - ansible-galaxy collection install -r requirements.yml
# - ansible-galaxy role install -r requirements.yml 
# This will install external roles & collections to the user's /home/<user>/.ansible/ dir